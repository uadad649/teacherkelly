# 유튜브 정보·오디오 — 유튜브가 자주 바뀌므로 항상 최신을 쓴다
yt-dlp

# 자막 받기 — 1.0 부터 프록시 설정을 받는다
youtube-transcript-api>=1.0

# 시트 만들기
anthropic>=1.0

# 자막이 아예 없는 영상까지 하려면 아래 주석을 푼다.
# 다만 모델을 내려받고 영상 길이만큼 CPU 를 쓰므로 실행이 크게 느려진다.
# faster-whisper
